{
  "name": "data-platform",
  "version": "3.4.0",
  "scripts": {
    "release": "release-it --ci --no-github.preRelease --no-github.release",
    "release:stage": "release-it --ci --no-increment --github.update",
    "release:prod": "release-it --ci --no-increment --no-github.preRelease --github.update",
    "hotfix": "./scripts/hotfix.sh && release-it --ci --no-github.preRelease && ./scripts/clean-up-hotfix.sh"
  },
  "devDependencies": {
    "@release-it/conventional-changelog": "^3.3.0",
    "release-it": "^14.11.6"
  }
}
