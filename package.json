{
  "name": "data-platform",
  "version": "1.12.0",
  "scripts": {
    "release": "release-it --ci",
    "hotfix": "./scripts/hotfix.sh && release-it --ci --no-github.preRelease && git push --delete origin hotfix"
  },
  "devDependencies": {
    "@release-it/conventional-changelog": "^3.3.0",
    "release-it": "^14.11.6"
  }
}
